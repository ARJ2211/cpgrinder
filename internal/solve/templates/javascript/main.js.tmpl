const fs = require("fs");

const input = fs.readFileSync(0, "utf8");
const tokens = input.trim().length ? input.trim().split(/\s+/) : [];
let idx = 0;

function next() {
  return idx < tokens.length ? tokens[idx++] : null;
}

function solve() {
  // TODO: parse + implement using next()
  return "";
}

const out = solve();
process.stdout.write(String(out));